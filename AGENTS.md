# AGENTS.md — INAPEL PQR System

## Project overview
Flask single-page app for PQR management (Peticiones/Quejas/Reclamos). Excel-backed DB (openpyxl). No SQL. No tests.

## Startup
```bat
INICIAR_PQR.bat   # launches `python app.py` + opens Chrome @ http://127.0.0.1:5000
```
Manual: `python app.py` in project root.

## Dependencies (pip)
```
flask openpyxl werkzeug reportlab
```
No `requirements.txt` — install manually if missing.

## Architecture
| File | Role |
|---|---|
| `app.py` | Flask entrypoint, creates `Base_Datos/Evidencias/`, calls `crear_excel()` + `actualizar_estructura_excel()` at import time |
| `routes.py` | Blueprint with 6 endpoints (`/api/pqr`, `/api/consultar/<valor>`, `/api/seguimiento`, `/api/cambiar_estado`, `/api/dashboard`, `/api/evidencias`) |
| `excel_db.py` | **Active DB layer**. All DB logic. Prints `>>> USANDO excel_db.py <<<` on import. Creates `BaseDatos_PQR.xlsx` in project root. |
| `pdf_generator.py` | Uses reportlab. Currently incomplete (`doc.build` called but no content added). |
| `templates/index.html` | Single-page app (inline CSS/JS, ~2270 lines). Password gate (`calidad2026`). No external JS files. |
| `templates/dashboard.html` | Standalone dashboard (unused — dashboard is inside index.html). |
| `Base_Datos/Evidencias/` | Upload directory for PQR evidence files, organized by radicado. |

## Dead / empty files
- `excel_db_corregido.py`, `excel_db_backup.py` — backup copies of DB layer (do not edit)
- `config.py`, `models.py`, `utils.py` — empty/unused
- `static/css/`, `static/js/` — empty (all styling/JS inline in index.html)
- `dashboard.html` — standalone version, not used (dashboard lives in index.html)

## Database
- **File**: `BaseDatos_PQR.xlsx` (auto-created in project root)
- **Sheets**: PQR, Historial, Investigaciones, Adjuntos
- **No migrations** — schema is created/updated on app start via `crear_excel()` / `actualizar_estructura_excel()`

## API endpoints
| Endpoint | Method | Notes |
|---|---|---|
| `/api/pqr` | POST | Save PQR + auto-save to localStore (dual persistence) |
| `/api/consultar/<valor>` | GET | Search by radicado, cliente, or nit |
| `/api/seguimiento` | POST | Save investigation data, auto-updates estado |
| `/api/cambiar_estado` | POST | Update estado + logs to Historial sheet |
| `/api/dashboard` | GET | Returns counts by estado/tipo/prioridad |
| `/api/evidencias` | POST | Multipart file upload to `Base_Datos/Evidencias/{radicado}/` |

## Known bugs / code issues
- Duplicate `obtener_dashboard()` in `excel_db.py:509` and `:550` — second overwrites first, and both stay in module scope
- `consultar_pqr()` in `excel_db.py` has indentation bugs (lines 297-316 are mis-indented relative to their intended loop)
- Duplicate JS functions in `index.html` (`dias()`, `priCls()`, `msg()` defined twice)
- `excel_db_corregido.py:actualizar_estado_pqr()` has broken indentation and stray debug prints
- When editing `excel_db.py`, watch for mixed function scoping (some code after `return` still executes at import time)

## Radicado format
`PQR-{year}-{consecutive:04d}` — auto-generated by `generar_radicado()` reading last row of PQR sheet.

## States
Recibido → Radicado → En revisión → En investigación → Pendiente de información → Pendiente de decisión → Acción en proceso → Respuesta enviada → Cerrado (or No procede).

Auto-transition: saving investigation sets estado to "En investigación" (or "Cerrado" if `cierre == "Sí"`).
